% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_promptbook.R
\name{read_promptbook}
\alias{read_promptbook}
\title{Read and validate a promptbook YAML file}
\usage{
read_promptbook(path)
}
\arguments{
\item{path}{Path to a YAML file.}
}
\value{
A \code{promptbook} object (S3 class), which is a named list with:
\itemize{
\item \verb{$title} — character
\item \verb{$version} — character or NULL
\item \verb{$description} — character or NULL
\item \verb{$author} — character or NULL
\item \verb{$prompt} — list with \verb{$system} and \verb{$user}
\item \verb{$variables} — list of \code{pb_variable} objects
\item \verb{$groups} — list of group definitions (or NULL)
}
}
\description{
Reads a YAML file, validates it against the promptbook schema, and returns
a structured \code{promptbook} object. All validation happens here — if this
function returns without error, the promptbook is valid.
}
\examples{
path <- system.file("examples", "media_framing.yaml", package = "promptbook")
pb <- read_promptbook(path)
pb
pb$title
pb$variables[[1]]$name
}
