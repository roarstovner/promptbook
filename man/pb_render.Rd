% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_render.R
\name{pb_render}
\alias{pb_render}
\title{Render a promptbook as a human-readable codebook}
\usage{
pb_render(promptbook, output = NULL, format = c("html", "typst"))
}
\arguments{
\item{promptbook}{A \code{promptbook} object (from \code{\link[=read_promptbook]{read_promptbook()}}) or a path
to a YAML file.}

\item{output}{Output file path. If \code{NULL}, generates a default name from the
promptbook title in the current working directory.}

\item{format}{Output format: \code{"html"} (default) or \code{"typst"} (PDF).}
}
\value{
The output file path (invisibly). Side effect: writes the rendered
document.
}
\description{
Renders a promptbook as a self-contained HTML or PDF (via Typst) document,
suitable for paper appendices and sharing with human coders.
}
\examples{
path <- system.file("examples", "media_framing.yaml", package = "promptbook")
pb <- read_promptbook(path)
if (interactive()) {
  pb_render(pb, "codebook.html")
}
}
